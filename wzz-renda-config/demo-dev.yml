# demo: ooo
# server:
#   port: 8103
# # 开启所有监控中心接口
# management:
#   endpoints:
#     web:
#       exposure:
#         include: "*"
server:
  port: 8200
zuul:
  routes:
    api-a:
      path: /api-member/**
      service-id: service-student
